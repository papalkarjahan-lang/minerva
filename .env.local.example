# Copy this file to .env.local and fill in your real values
# NEVER commit .env.local to GitHub

# Supabase - from your project dashboard > Settings > API
VITE_SUPABASE_URL=https://yourproject.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Mapbox - from mapbox.com > Tokens
VITE_MAPBOX_TOKEN=pk.eyJ1IjoieW91cnVzZXJuYW1lIiwi...

# Your app URL (used for tracking links in SMS)
VITE_APP_URL=https://minerva-green.vercel.app

# Internal admin console (2026-09-05) - comma-separated list of Minerva
# staff emails allowed to open /admin. Leave unset and /admin shows
# "Not authorized" to everyone.
VITE_ADMIN_EMAILS=you@yourcompany.com

# Optional error monitoring (2026-09-05) - leave unset and this is a no-op.
# Get a free DSN at sentry.io.
VITE_SENTRY_DSN=

# Business contact footer (2026-09-08) - shown on the public landing page
# footer. All optional and independently shown/hidden - leave any of these
# unset and that part of the footer line is simply omitted (never shows a
# broken "[your ABN]"-style placeholder).
VITE_BUSINESS_ABN=
VITE_BUSINESS_EMAIL=
VITE_BUSINESS_PHONE=

# Stripe publishable key (2026-09-14) - from Stripe Dashboard > Developers >
# API keys. Safe to expose client-side (it's the "publishable" key, not the
# secret key). Powers the optional "Pay now" card-payment button on
# InvoiceView; leave unset and that button simply doesn't render, invoices
# stay on the existing manual "mark paid" flow.
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...
